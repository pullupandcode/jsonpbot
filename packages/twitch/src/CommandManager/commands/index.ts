import theme from "./theme";
import hue from "./hue";
import so from "./so";
import commands from "./commands";
import uptime from "./uptime";

export default {
  hue,
  theme,
  so,
  commands,
  uptime,
};
